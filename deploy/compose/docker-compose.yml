services:
  api:
    image: python:3.11-slim
    working_dir: /app
    command: bash -lc "pip install -e '.[dev]' && uvicorn api.main:app --host 0.0.0.0 --port 8000 --reload"
    ports:
      - "8000:8000"
    volumes:
      - ../..:/app
    env_file:
      - ../../.env
